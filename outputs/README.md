# Outputs

This directory is used for generated reporting outputs produced locally during the workflow.

## Typical Outputs

- Summary JSON files for KPI and distribution checks
- Dashboard export artifacts when available
- Validation reports from the data quality stage

## Notes

The `outputs/` directory is excluded from git by default and is intended for local verification and portfolio demonstrations.
